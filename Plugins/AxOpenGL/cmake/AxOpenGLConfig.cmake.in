@PACKAGE_INIT@

find_package(OpenGL REQUIRED)
find_package(Foundation CONFIG REQUIRED)
find_package(Drawable CONFIG REQUIRED)

if(NOT TARGET Virspace::AxOpenGL)
	include(${CMAKE_CURRENT_LIST_DIR}/AxOpenGLTargets.cmake)
endif()