@PACKAGE_INIT@

find_package(Foundation CONFIG REQUIRED)
find_package(GTest REQUIRED)

if(NOT TARGET Virspace::AxResourceTests)
	include(${CMAKE_CURRENT_LIST_DIR}/AxResourceTestsTargets.cmake)
endif()