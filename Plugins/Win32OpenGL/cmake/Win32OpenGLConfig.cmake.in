@PACKAGE_INIT@

find_package(OpenGL REQUIRED)
find_package(Foundation CONFIG REQUIRED)
find_package(Drawable CONFIG REQUIRED)

if(NOT TARGET Virspace::Win32OpenGL)
	include(${CMAKE_CURRENT_LIST_DIR}/Win32OpenGLTargets.cmake)
endif()