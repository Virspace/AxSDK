/**
 * AxScript.cpp - Script base class implementation
 *
 * Most methods are inline in the header and delegate to singletons:
 * - Input methods delegate to AxInput
 * - Scene methods delegate to AxSceneManager
 */

#include "AxEngine/AxScript.h"

// Currently empty - all methods are inline in the header.
// This file exists for future non-inline implementations if needed.
